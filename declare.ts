declare namespace jQuerya{
    function ajax(url:string,methods:'post'|'get'):void;
    class app{
        name:number;
    }
}
let arrs:Array<string>
declare class Animal{
    name:string;
    constructor(name:string);
    sayHi():string
}
let add = new Animal('lin')
export default {jQuerya, arrs}
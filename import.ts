import {jQuerys} from './function'
import set from './declare'
let p:jQuerys.get = {
    methodes: 'post',
    data: {
        name: 'foo'
    }
}
set.arrs
set.jQuerya.ajax('12','get')
toStrings(12)
declare namespace sp{
    const add:string;
    function ajax(url:string,methods?:Array<string>,...arr:Array<number>)
}